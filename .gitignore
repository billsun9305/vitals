/target
/dashboard/node_modules
/dashboard/dist
/dist
*.app
/.superpowers
/.claude/
.DS_Store
*.log

# Release credentials never enter the tree: an exported Developer ID
# certificate (.p12) or an App Store Connect API key (.p8) placed here by
# mistake stays uncommitted.
*.p12
*.p8
