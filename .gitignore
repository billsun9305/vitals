/target
/dashboard/node_modules
/dashboard/dist
/dist
*.app
/.superpowers
/.claude/
.DS_Store
*.log

# Release credentials live here and nowhere else in the tree: the exported
# Developer ID certificate (.p12) and the App Store Connect API key (.p8)
# that scripts/release-secrets.sh reads. Never committed, never uploaded.
/.secrets/
*.p12
*.p8
