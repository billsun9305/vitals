[workspace]
resolver = "2"
members = ["crates/core", "crates/app"]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"
authors = ["Bill Sun"]
repository = "https://github.com/billsun9305/vitals"
homepage = "https://github.com/billsun9305/vitals"
keywords = ["macos", "apple-silicon", "monitoring", "menubar", "cli"]
categories = ["command-line-utilities", "os::macos-apis"]

[workspace.dependencies]
vitals-core = { path = "crates/core" }
macmon = { version = "0.8", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
time = { version = "0.3", default-features = false, features = ["std", "formatting"] }
sysinfo = "0.39"
libproc = "0.14"
clap = { version = "4", features = ["derive"] }
objc2 = "=0.6.4"
objc2-foundation = "=0.3.2"
objc2-app-kit = "=0.3.2"
objc2-web-kit = "=0.3.2"
block2 = "=0.6.2"
tiny_http = "0.12"
flate2 = "1"
tar = "0.4"
# 0.11 needs Rust 1.85; the workspace floor is 1.82.
sha2 = "0.10"
# tempfile pulls in getrandom; getrandom 0.4 needs Rust 1.85, so Cargo.lock
# holds it at 0.3 — `cargo update` must keep it there.
tempfile = "3"
objc2-security = "=0.3.2"
objc2-service-management = "=0.3.2"
objc2-core-foundation = "=0.3.2"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
opt-level = "s"
